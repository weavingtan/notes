# 本项目规则（weaving/www/notes）

> 这份文件记录**本仓库上真实踩过的坑**，避免重复犯。
> 每条都对应一次实际事故，不是泛泛建议。改完代码、宣布"完成"之前，先过一遍 §9。

---

## 1. 验证纪律（最重要，多次翻车都在这）

**禁止在验证覆盖不到自己声明之前说"已修复 / 已一致 / 已通过"。测试全绿 ≠ 功能可用。**

真实事故：`npm test` 13/13 全绿，但全站 JS 因一处语法错误被浏览器**整块丢弃**，
主题切换、日夜模式、Cmd+K 搜索、代码复制**全部失效**。测试一条都没拦到，
因为当时的断言全是「字符串包含」。

断言必须覆盖**你声称的那件事**：

| 你的声明 | 必须有的证据 |
|---|---|
| 「脚本正常」 | 真正**解析**脚本（`new Function(code)` / `node --check`），不是子串匹配 |
| 「样式/结构一致」 | 比较**几何**（元素个数、盒尺寸），不是文本相似度（见 §2） |
| 「链接可用」 | `npm test` 的 404 扫描器实际解析过 |
| 「视觉正常」 | 抽 `dist/` 里的**产物**核对，不是读源码推理 |

---

## 2. 跨页视觉一致性怎么验（血泪教训）

5 个页面壳共用 header / footer。**正确方法**：

1. 从**产物**抽出两页的 `<header>`（或 `</main>` 之后区段）到临时文件；
2. **先数可变元素个数**：`nav-action-btn`、`theme-pill`、图标…… 这些决定几何；
3. 再比 CSS 盒属性：`height` / `max-width` / `padding` / `gap`。

**反例（我犯过）**：把 `active` 高亮、空行、类名统统归一化后做文本 diff，
得出「header 完全一致」的结论。实际上文章页 header 多了**一个 42px 按钮**，
而 `.nav-container` 是 `justify-content: space-between` →
导航项被**横向推移约 21px**，跨页跳转时顶部肉眼可见地跳动。

**规则：任何归一化都可能掩盖真实差异。归一化之前先问：这个差异会不会影响布局？**

---

## 3. `scripts/build.mjs` 的模板字符串陷阱

本站所有产物（HTML/CSS/JS）都写在 `build.mjs` 的**模板字符串**里。

- **模板字符串会把 `\"` 吞成 `"`。**
  - 想让产物出现 `\"`，源码必须写 `\\"`；
  - **更稳的做法是绕开**，例如转义表用单引号做键：`'"': "&quot;"`。
- 事故：写了 `"\"":"&quot;"` → 产物变成 `"""` → SyntaxError → 整块 `<script>` 被丢弃。
- **`edit` 报"成功替换"不代表产物正确**：JSON 里的 `\"` 落到文件里只是 `"`。
  **改完必须看构建产物。**
- 新增/修改内联脚本后，`npm test` 的 Test 14 会解析全部内联脚本，务必让它跑到。

---

## 4. 用户用标签指代区块时，先确认它在哪个页面

事故：用户说「ARCHIVE 的背景图不好看」，我删了**归档页**的 Hero 配图；
用户实际指的是**首页** Chapter 4 的 `ARCHIVE ——` 带。

事故：用户说「太单调了」，我理解成「不要背景」，实际意思是「别和底栏用同一张图」。

**规则**：用户用 `ARCHIVE` / `与我交流` / `标签页` 这类标签指代时，
先 `grep` 该标签，列出它出现在哪些页面与函数，确认后再动手；**不确定就问**。

---

## 5. 不要整块回滚「看不懂的改动」

事故：我把一批工作区变更判定为「子代理污染」整块 `git checkout` 回滚，
其中夹着**正当修复**（`.site-nav` 白字压浅色底导致看不清）。

**规则**：逐 hunk 审。确认是垃圾才删；是修复就留。混合 diff 不能一把梭。

---

## 6. 子代理不能信为「只读」

`pi -p` 起的子代理有 `bash`，**提示词里写"只读"它照样能改文件**（本会话发生过）。

**规则**：子代理跑完先 `git status` / `git diff` 再信工作区；
更稳的是跑之前 `cp` 一份源码快照，事后比对。

---

## 7. 我看不了图（本会话模型限制）

用「支持视觉」的模型转述截图会**幻觉**——曾输出与本站毫无关系的内容
（"Cloud Burst" / "Build faster with AI"），我据此差点改错代码。

**规则**：直接告诉用户"读不了图"，请其用文字描述：
**哪个页面 / 哪个位置 / 横向还是纵向 / 什么在动**。
**绝不基于幻觉描述改代码。**

---

## 8. 项目硬事实（别猜，照做）

- **微信防塌陷硬红线**：`<main>` 与 `.article-content` 内 **0 个 `<div>`**。
  新增区块一律用 `<section>`。
- **流式视口架构**：容器是 `min(94vw, 1280px)`。
  **禁止对 `.main-container` / 文章容器写刚性 `max-width: 760px/860px`**（测试会拦）。
- **页面体系**：`index` / `archives` / `categories` / `about` 四页 + 文章详情页。
  标签页已并入分类页，不要再复活 `tags.html`。
- **顶栏只有一个类** `.page-header`（全站逐字节一致）。不要再引入 `site-nav` /
  `article-page-header` 这类变体；也不要在 header 里加只在**部分页面**出现的按钮
  —— 按钮数量一变，导航就会横向位移（见 §2）。
- `feed.xml` 由构建生成。**子目录页面（`dist/posts/`）引用它必须加 `../`**。
- 改 `--header-height` 时**同步 `html { scroll-padding-top }`**
  （现已写成 `var(--header-height)`，别再写死像素）。
- 主题色 token 定义在 `SITE_STYLES` 的 `:root` 与 `[data-theme][data-mode]`。
  **`--primary` 在浅色主题下对白底只有 2.54:1，禁止拿它当小号正文颜色**
  （按钮底、图标、背景、hover 可以）。
- 5 套主题都有 `--primary-hover`：浅色模式下更深、深色模式下更亮，
  需要"更清楚的主题色文字"时优先用它。

---

## 9. 本仓库反复出现的缺陷类型（每次改 UI 必查一遍）

这个站的历史里，同一类「看上去能用、实际没用」的东西反复出现。改动 UI 时主动扫一遍：

| 缺陷类型 | 实例 | 判断方法 |
|---|---|---|
| **假控件** | 假 Tabs（最新/最热/最多阅读）、假分页 1–5、FEATURED 的 `↑ ↓` 箭头、写死的 `01 / 04` 计数 | 有 `cursor:pointer` / `:hover` 但**没有 click 处理器**或点了不改变任何状态 → 删掉，或做成真的 |
| **幽灵 CSS token** | `--font-serif` 被引用 6 次却从未定义，全部静默回退到只有拉丁字形的 Georgia | 引用的 `var(--x)` 必须有定义。**Test 15 已拦**（`var()` 自带 fallback 不会报错，字符串断言看不出来） |
| **死 token** | `--font-calligraphy` 定义了但引用 0 次（字体被下载却从不生效）；`--bg-hover` / `--accent-primary` / `--accent-blue` | 定义了没人用 → 要么接上，要么删掉 |
| **硬编码占位内容** | `DESIGN / TECHNOLOGY / LIFE / NOTES`（不是本站分类）、`"Good design makes life better."`、`2026 · Pro`、`01 / 04` | 站自称「零硬编码」，任何写死的分类名/数量/文案都要改成从数据算 |
| **href 指了锚点但页面不读 hash** | 首页/归档页链接 `categories.html#cat-技术`，但分类页 `location.hash` 读取次数 = 0 → 点进去不筛选 | 生成深链时，**同时确认目标页真的读取它**；只改 href 是无效修复 |
| **中文加 `font-style: italic`** | 中文引语 / `BEIJING · 2026` 类文本 | 浏览器会对 CJK 做**伪斜体**，看起来是坏的。中文用 `--font-calligraphy` 或正体 |
| **对比度不达标** | `--primary`（浅色下 2.54:1）被当文字色；`--text-light`（2.6:1）当正文 | 小于 4.5:1（或大号文字 3:1）就不能当文字色。**Test 3 只检正文，不检这些** |

### 已有的自动门禁（别重复交这种学费）

| Test | 拦什么 |
|---|---|
| 2 | 404 死链（含子目录 `/../` 前缀） |
| 5 | `<main>` / `.article-content` 内出现 `<div>` |
| 9 | 刚性 `max-width: 760/860px` |
| 14 | 内联脚本语法非法 |
| 15 | CSS 变量被引用但未定义 |

## 10. 交付前自检清单（逐条打勾再回复用户）

- [ ] `npm test` 全绿（现为 **15** 套），**且新增断言覆盖了我声称的那件事**
- [ ] 抽 `dist/` 的**真实产物**核对该改动确实生效
- [ ] **真的执行过**关键交互路径（如深链：取产物里的脚本来跑，而不是 grep 字符串）
- [ ] 内联脚本语法合法（Test 14）与 CSS 变量完整（Test 15）
- [ ] 404 扫描 0 死链
- [ ] 跨页一致性：**数过可变元素个数**，不是比文本相似度（见 §2）
- [ ] 对照 §9 扫一遍：假控件 / 幽灵 token / 硬编码占位内容 / 未读取的 hash / 中文伪斜体 / 对比度
- [ ] 没留下死代码 / 死参数（`grep` 新引入的名字确认有引用）
- [ ] 没把死类留在 CSS 选择器里
- [ ] 没有在**验证覆盖不到**的地方宣布"完成"
